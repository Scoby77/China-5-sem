<?php
/**
 * Export to PHP Array plugin for PHPMyAdmin
 * @version 5.2.1
 */

/**
 * Database `drangonball`
 */

/* `drangonball`.`characters` */
$characters = array(
);

/* `drangonball`.`character_transformations` */
$character_transformations = array(
);

/* `drangonball`.`tournaments` */
$tournaments = array(
);

/* `drangonball`.`tournament_participants` */
$tournament_participants = array(
);

/* `drangonball`.`transformations` */
$transformations = array(
);

/* `drangonball`.`users` */
$users = array(
);
